<div class="container">
  <h1>Add new list</h1>
  <%= simple_form_for [ @list] do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Add list", class: "btn btn-primary" %>
  <% end %>
</div>
