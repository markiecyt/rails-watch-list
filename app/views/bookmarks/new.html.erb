<div class="container">
  <h1>Bookmarks</h1>
   <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.submit "Add to bookmark", class: "btn btn-primary" %>
  <% end %>
</div>
